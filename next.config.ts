import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Фото тканей/изделий и лекала могут быть крупными — поднимаем лимит
  experimental: {
    serverActions: {
      bodySizeLimit: "25mb",
    },
  },

  /**
   * Доступ к dev-серверу с других устройств в той же сети (планшет, телефон).
   * По умолчанию Next пускает только localhost и блокирует всё остальное.
   * Разрешаем частные диапазоны домашних и офисных сетей — этого достаточно,
   * чтобы открыть приложение на планшете по адресу вида http://192.168.1.42:3000
   *
   * Это касается ТОЛЬКО режима разработки. В продакшене приложение работает
   * по своему адресу, и этот список не используется.
   */
  allowedDevOrigins: [
    "192.168.*.*",
    "10.*.*.*",
    "172.16.*.*",
    "172.17.*.*",
    "172.18.*.*",
    "172.19.*.*",
    "172.2*.*.*",
    "172.30.*.*",
    "172.31.*.*",
    "*.local",
  ],
};

export default nextConfig;

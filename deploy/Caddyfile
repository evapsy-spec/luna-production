luna.evamoon.boutique {
	encode zstd gzip

	# MCP-коннектор: отдельный процесс на 3100, доступ по токену внутри него.
	# Стоит выше общего обработчика, иначе запрос уйдёт в приложение.
	handle /mcp* {
		reverse_proxy 127.0.0.1:3100
	}

	# Клиент MCP может искать здесь OAuth. Уводим на тот же сервис, чтобы он
	# получил честный 404, а не форму входа приложения с последующим 404.
	handle /.well-known/oauth* {
		reverse_proxy 127.0.0.1:3100
	}

	handle {
		reverse_proxy 127.0.0.1:3000
	}

	header {
		Strict-Transport-Security "max-age=31536000"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "SAMEORIGIN"
		Referrer-Policy "strict-origin-when-cross-origin"
	}
}
